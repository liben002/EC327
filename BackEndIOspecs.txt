Inputs:

Board board = new Board(boolean isMulti, Location[] squareLocs);
	Takes:
	A boolean if is multi-player;
	An array of Locations for where the squares exist

	*Location loc = new Location(int x, int y);
		Takes:
		Two integers to specifiy a location on the screen

board.updateBoardState(int pieceIndex, int steps);
	Takes:
	An integer that is the index of the piece to be moved
	An integer that is the number of steps to move the piece


Outputs:

Piece[] pieces = board.getPieces();
	Returns:
	An array of pieces(use the index of this array to update board)

Location pieceLoc = pieces[i].getScreenLoc();
	Returns:
	A piececs location on the screen